# Project

- 项目名称为 `CAN: Lattice-Based Neural Model Access Control`，研究将格密码验证关系编译为固定神经网络，并用其结果控制 CNN/DNN 及后续 MoE/多智能体工具能力。
- 项目面向密码学、机器学习安全和访问控制研究者，以及需要复现实验和审阅论文结论的用户；目标是科研论文和可复现实验，不是生产访问控制产品。
- 阶段 A 研究精确 oracle、toy LWE 数值解锁、定点/量化神经验证器和黑盒业务模型硬门控；阶段 B 研究认证证据、唯一授权协调器、短期 capability 和工具网关。
- 首篇论文以阶段 A 的形式化关系、误差界、soundness-preservation 和最小 CNN/DNN 原型为核心。阶段 B 是后续扩展，不得在阶段 A 未闭合时擅自提前扩大范围。
- 明确非目标：首版不实现完整 ML-DSA，不声称 toy LWE 解密等同数字签名，不解决白盒权重读取/修改、TEE、安全启动、远程证明或完整侧信道，不把实验结果描述为生产安全保证。
- 本项目属于安全相关研究，只允许防御性实现和验证；不得实现攻击性后门、规避检测、未授权访问、凭据窃取或可直接用于攻击第三方系统的利用功能。
- 项目的动态事实、当前目标和唯一下一步以根目录 `PROJECT_WORKLOG.md` 为准。若未来启用多个 worktree，则主工作树中的该文件是跨分支唯一事实来源。
- 研究定义、阶段边界和论文主张以 `docs/RESEARCH_DESIGN.md` 为准，A0 精确 relation 和编码以 `docs/A0_PROTOCOL_SPEC.md` 为准，信任模型和明确不保证的性质以 `SECURITY.md` 为准。

# Session workflow

- 每次开始工作时，先阅读所有适用的 `AGENTS.md`、根目录 `PROJECT_WORKLOG.md` 以及与当前任务相关的设计、安全和任务文档。
- 检查 `git status`、`git branch --show-current`、`git worktree list --porcelain` 和当前完整 `HEAD`。如果目录尚不是 Git 仓库，明确记录该事实，不得虚构分支或 commit。
- 修改前先从工作日志确认当前目标、已完成内容、阻塞项和唯一下一步；事实不完整时先通过代码、配置和测试核实。
- 用户改变目标时，先更新 `PROJECT_WORKLOG.md` 中的目标、任务状态和唯一下一步，再修改实现。
- `PROJECT_WORKLOG.md` 的唯一下一步必须同时标记计算资源状态。当前步骤可由本机完成时标记
  `LOCAL_OK`；当下一步首次进入 V1-M1 GPU 环境冻结、CIFAR-100/ResNet-18 正式训练或其他已确认
  必须使用服务器的工作时，先改为 `SERVER_REQUIRED` 并通知用户，再安装服务器环境、下载正式
  训练数据或启动论文训练。不得在通知前产生正式服务器实验结果。
- 每次工作遵循：检查现状 -> 实现 -> 测试 -> 更新文档 -> checkpoint 总结。
- 代码、配置和可复现的测试结果优先于过时文档。发现差异时，以实际实现为准，并在工作日志中记录差异及后续修正文档的任务。
- 不得在只完成部分实现、没有完成相称验证或文档已经过期时宣称任务完成。
- 工作持续较长时，定期向用户报告当前发现、正在进行的工作和剩余风险。
- 上下文接近上限时，主动更新工作日志、保存可恢复的 checkpoint，并建议在新会话中从该 checkpoint 继续。

# Engineering rules

- 使用仓库已经选择的语言、版本、框架、格式、helper、接口、抽象和设计模式。仓库尚未作出选择时，不得仅凭偏好引入技术栈。
- 优先采用小而易审查的 patch。不重写无关模块，不格式化无关文件，不做与当前目标无关的重构。
- 仅当抽象能减少真实复杂度、消除明显重复或匹配已有架构时才新增抽象。
- 结构化数据必须使用正式解析器或结构化 API，不得使用脆弱的字符串拼接或正则模拟解析。
- 所有公开 API 都必须有类型标注和 docstring。新建或实质修改的公开函数使用简洁中文 docstring。
- 私有 helper 仅在复杂逻辑、边界条件或协议语义不直观时添加简洁中文注释；不给显而易见的代码机械添加注释，不使用“中文：”等语言标签前缀。
- 测试尽量使用显式、确定性的种子，并记录影响复现的环境条件。
- 不提交秘密、凭据、私钥、本地数据库、模型 checkpoint、生成输出或大型二进制文件。
- 测试生成的密钥和临时数据必须放在测试框架提供的临时目录中，并在测试后清理。
- 不引入保存密钥、权限状态或安全上下文的全局可变状态。
- 严格验证所有外部输入。未知字段、重复字段、错误长度、非有限数值、非规范编码和类型混淆默认拒绝。
- 对安全关键字段使用精确类型判断，不得依赖 truthy/falsy 隐式转换。
- 默认行为必须 fail closed；任何降级模式都必须显式配置、可审计且默认关闭。

# Threat-model alignment

本节与 fail-closed 规则同等强度，用于防止过度防御。本项目面向科研论文而非生产，过度防御会
拖慢迭代且不增加可发表结论，与上文“小而易审查 patch”“仅当抽象减少真实复杂度时才新增抽象”
一致。

- 每一处输入校验、类型检查、运行时断言都必须对应 `SECURITY.md` `## Attacker capabilities`
  中列出的某个攻击者能力。针对 `SECURITY.md` `## Explicitly unsupported guarantees` 明确
  不防御的能力（白盒读取/修改权重、修改推理代码、删层、剪枝、微调、宿主机控制、侧信道）
  增加运行时防御属于过度防御，禁止加入。
- 可信且不可变的配置（模型拓扑、权重、compiled profile、registry、policy）只在构造或加载时
  校验一次；禁止在每次请求处理路径中重复校验这些可信对象是否被篡改。构造后不可变的前提由
  冻结和类型约束保证，不由每请求重扫权重 shape/dtype/hook 保证。
- 严格类型、编码、shape、范围校验只施加于解析不可信外部输入的 parser/入口层；对本项目自身或
  依赖库（如 PyTorch）契约已经保证的内部返回值，不重复防御性检查。
- 实验测量代码（计时、计数、采样、latency 统计）属于实验关注点，放在 `experiments/` 或通过
  外部包装/钩子采集，不写入 `access/` 等安全核心模块的请求路径。
- 新增任何校验或防御分支前自问：它防御的攻击者在当前威胁模型内吗？不在则不写。不为威胁模型
  之外的假想攻击者增加分支、抽象或每请求开销。
- 收紧防御以外，也要主动移除已存在的过度防御：发现某校验只防御 unsupported guarantees 或只
  重复可信不变量时，在相应 checkpoint 连同其测试一并简化，并在工作日志记录依据。

# Architecture and authorization rules

如果项目包含认证或授权，下列流程是默认且必须由设计、实现和测试共同保持的架构：

```text
不可信输入
-> 规范化解析
-> 无副作用的确定性验证
-> 只包含证据、不具有授权能力的结构化结果
-> 唯一协调器提交授权决定
-> 生成已提交的 capability/context
-> 执行受保护副作用
```

- 请求方不能直接提交 `allow`/`deny` 结果，也不能创建授权 context 或 capability。
- 请求方不能选择更弱的验证路线、算法或安全策略。
- 密钥、算法、profile 和策略必须由本地可信配置选择。
- 验证器只产生证据，不直接产生权限；只有协调器可以提交最终授权并创建授权上下文。
- replay、tamper、格式错误、权限提升和验证失败必须产生零受保护副作用。
- 禁止把多个安全路线实现为“A 或 B”式弱回退，除非安全设计明确证明该组合允许且测试覆盖其边界。
- 拒绝原因、验证 trace 和审计结果必须稳定、结构化且可测试，同时不得泄露秘密或可被利用的敏感细节。
- 项目 A0 的 toy LWE 解密只能称为“数值解锁”或“toy capability gate”，不能称为验签、身份认证或不可伪造方案。
- `A`、`q`、算法、阈值、噪声 profile、量化 scale 和验证参数必须由本地可信 registry 固定；请求方不得通过输入选择或覆盖。
- A0-v1 只接受 `docs/A0_PROTOCOL_SPEC.md` 定义的 23 字节 credential；`A_slot` 由 `(profile_id, slot_id)` 从本地 registry 解析，wire input 不含矩阵或参数。
- 必须测试 `A = 0`、chosen `b`、decryption oracle 和公开加密任意铸造授权密文等语义攻击，不能只测试随机噪声。
- 安全承载版本优先使用公开验证信息。真实私钥必须留在模型和 verifier 之外；验证器只产生证据，由协调器提交权限。
- 神经 soundness 的最低目标是对全部可表示输入满足 `V_nn(a) = 1 -> V_ref(canonical(a)) = 1`。有限随机测试不能替代该包含关系的证明。
- MoE Router、LLM、规划专家和业务专家均不是权限提交点；所有受保护工具调用必须经过 capability 与真实参数匹配的工具网关。

# Cryptography rules

如果项目涉及密码学，必须遵守以下规则：

- 不手写生产级密码算法。生产实现通过边界清晰的 adapter 接入经过审查和持续维护的密码库。
- toy 或研究实现必须明确标注为非生产用途，并与生产路径隔离。
- 私钥不得进入 verifier、模型、日志、全局状态或客户端可控输入。
- toy LWE 实验只能使用临时生成的非生产测试参数，不得把 secret、测试密钥或含 secret 的模型 artifact 提交到仓库。
- 验证只能依赖公开信息和本地可信策略。
- 密码算法、参数集和密钥用途必须由本地信任注册表确定；不允许请求方通过输入实现算法或策略降级。
- 密码验证失败默认执行 `reject`、`drop` 和/或安全审计，不得继续受保护操作。
- 在已有格密码关系可以满足研究问题时优先研究已有方案；未经完整定义、安全游戏和归约，不得把自定义 margin 关系称为安全数字签名。

# Testing rules

- 安全边界必须包含正向和负向测试。
- 测试至少覆盖：合法输入、非法输入、边界值、类型混淆、重放、篡改、并发或重复提交，以及失败时无受保护副作用。
- 优先运行与改动直接相关的最小测试，再运行完整测试套件。
- 必须从仓库配置而非猜测中识别命令，并在 `PROJECT_WORKLOG.md` 记录实际运行的准确命令和结果。
- Python 质量配置以根目录 `pyproject.toml` 为准。当前经过实际运行的准确命令为：

| 检查类型 | 准确命令 |
| --- | --- |
| 单元测试 | `.venv/bin/python -m pytest tests/unit` |
| 集成测试 | `.venv/bin/python -m pytest tests/integration` |
| 安全测试 | `.venv/bin/python -m pytest tests/security` |
| 完整测试 | `.venv/bin/python -m pytest` |
| lint | `.venv/bin/ruff check .` |
| 格式检查 | `.venv/bin/ruff format --check .` |
| 类型检查 | `.venv/bin/mypy src tests` |
| 依赖一致性 | `.venv/bin/python -m pip check` |
| 检查脚本语法 | `bash -n scripts/check_governance_docs.sh` |
| 治理文档检查 | `./scripts/check_governance_docs.sh` |

- `tests/security` 当前只验证 bootstrap 的敏感产物忽略配置；实现任何密码、认证或授权边界时，必须在同一 checkpoint 中加入相应正负向安全测试，不能把 bootstrap 测试描述为业务安全验证。
- 修改 `AGENTS.md`、`PROJECT_WORKLOG.md`、`SECURITY.md`、`docs/RESEARCH_DESIGN.md` 或 `docs/A0_PROTOCOL_SPEC.md` 后，必须运行 `./scripts/check_governance_docs.sh`。
- 无法运行的测试必须在最终总结和工作日志中明确说明原因，不得用“应当通过”代替结果。

# Git and worktree safety

- 可以执行只读 Git 检查。保留用户已有改动，不得覆盖、回滚、删除或格式化无关文件。
- 不使用 `git reset --hard`、`git checkout --` 或其他破坏性命令，除非用户对准确目标明确授权。
- 每次会话结束前准备 commit-ready checkpoint 摘要；提交前必须展示准确的待提交文件列表。
- 文件中包含秘密、凭据、数据库、模型输出、论文私有材料或大型文件时，不得自动提交或推送。
- 默认不得推送主开发分支。用户明确要求备份时，只推送到 `backup/<current-branch>` 专用分支。
- 未经用户明确要求，不创建 PR、不合并分支、不发布版本、不推送任何分支。
- 多 worktree 项目必须指定主工作树中的 `PROJECT_WORKLOG.md` 为跨分支唯一事实来源。
- 非主工作树形成 checkpoint 后，必须把完整 `HEAD`、准确测试命令与结果、工作树状态和残余风险同步到主工作树工作日志；不得为了同步文档而把功能分支合并到主分支。
- 如果仓库存在 worktree 同步检查脚本，每次结束前必须运行并记录结果。

# Definition of done

任务只有在以下条件全部满足时才算完成：

1. 请求的行为已经实现。
2. 相关测试通过。
3. 新模块具有单元测试。
4. 安全不变量已经记录。
5. 工作文档已经更新并与代码、配置和 Git 状态一致。
6. 实现范围、未实现内容、测试结果和残余风险已经总结。
7. toy、实验性、单机假设、小样本结论和非生产限制均有明确标记。
8. 多工作树状态与主工作日志一致。
9. 已形成可提交的 checkpoint，并展示准确的待提交文件列表。
